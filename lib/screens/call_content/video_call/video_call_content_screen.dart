import 'package:flutter/material.dart';

class VideoCallContentScreen extends StatelessWidget {
  const VideoCallContentScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text("VideoCallContentScreen")),
    );
  }
}
